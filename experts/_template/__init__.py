"""The generic example expert — a template to copy when starting a new domain.

It is a working skeleton, not a proven domain: its ``ask()`` is a deterministic
stub that loads playbooks and keyword-matches, proving the CLI contract end to
end without any model calls. See DEV.md for how to grow it and how to upgrade
``ask()`` into a real claude-agent-sdk agent.
"""
